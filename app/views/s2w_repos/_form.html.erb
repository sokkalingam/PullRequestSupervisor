<%= form_for(@s2w_repo) do |f| %>
  <% if @s2w_repo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@s2w_repo.errors.count, "error") %> prohibited this s2w_repo from being saved:</h2>

      <ul>
      <% @s2w_repo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
