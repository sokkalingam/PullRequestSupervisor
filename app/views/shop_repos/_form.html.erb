<%= form_for(@shop_repo) do |f| %>
  <% if @shop_repo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_repo.errors.count, "error") %> prohibited this shop_repo from being saved:</h2>

      <ul>
      <% @shop_repo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
